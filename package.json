{
  "name": "auto-skills-scanner",
  "version": "0.1.0",
  "description": "AUTO Skills Scanner — Agent Skills supply chain security scanner (VirusTotal for Agent Skills)",
  "main": "dist/cli.js",
  "bin": {
    "auto-scanner": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "scan": "node dist/cli.js scan"
  },
  "keywords": ["agent", "skills", "security", "scanner", "supply-chain", "mcp"],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": { "node": ">=18" }
}
