{
  "name": "auto-skills-scanner",
  "version": "0.1.2",
  "description": "Scan any AI Agent Skill or MCP server for malware before you install it — the VirusTotal for the Agent ecosystem. Zero deps, 65kB, 8 threat categories.",
  "main": "dist/cli.js",
  "bin": {
    "auto-scanner": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "scan": "node dist/cli.js scan"
  },
  "keywords": ["agent", "skills", "security", "scanner", "supply-chain", "mcp"],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "engines": { "node": ">=18" }
}
